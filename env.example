# Django Settings
SECRET_KEY=your-secret-key-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
# For Docker: postgresql://postgres:postgres@localhost:5432/conheces_alguem
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/conheces_alguem

# Email Configuration (Optional - MVP uses console backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@conhecesalguem.ao

# AWS S3 Configuration (Production - Optional)
# Only needed if using S3 for media files
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=us-east-1
USE_S3=False

# Phone Verification (Manual WhatsApp - Phase 1)
# In future, integrate WhatsApp Business API
WHATSAPP_VERIFICATION_ENABLED=True

# Admin
ADMIN_EMAIL=admin@conhecesalguem.ao

# Frontend
FRONTEND_URL=http://localhost:8000

# Currency
DEFAULT_CURRENCY=AOA

